;; Generated by protoc-gen-clojure from fixtures/wire/p2.proto. Do not edit.
;;
;; The FileDescriptorProto below is embedded verbatim; protobuf-java resolves
;; its edition features at load time, so this file needs no regeneration when
;; a new edition ships.
(ns interop.fixtures.wire.p2
  (:require [clj-protobuf.codec :as codec]
            [clj-protobuf.runtime :as rt]))

(def ^:private descriptor-b64
  "")

(def file-descriptor
  (rt/file-descriptor descriptor-b64
                      []))

;; ---------------------------------------------------------------
;; messages
(declare Leaf->proto Wire->proto Wire-Grp->proto Wire-Grps->proto Required->proto proto->Leaf--map proto->Leaf--slot-map proto->Wire-Grp--slot-map proto->Wire-Grps--slot-map)
;;
;; The shape is known at codegen time, so the representation is too:
;; a defrecord per type, its FieldDescriptors resolved once into
;; vars, and straight-line ->proto/proto-> built on them. nil means
;; absent, which is how a record (all keys always present) maps onto
;; protobuf explicit presence.

(defrecord Leaf [id])
(def Leaf-prototype (rt/message file-descriptor "Leaf" "com.acme.fixtures.wire.p2.Leaf"))
(def ^:private Leaf--id (rt/field Leaf-prototype "id"))
(defn Leaf->proto
  "Clojure -> protobuf. Takes the Leaf record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Leaf->proto m nil))
  ([m opts]
   (if (nil? opts)
     (let [b (com.acme.fixtures.wire.p2.Leaf/newBuilder)]
       (when-some [v (:id m)] (.setId b ^String v))
       (.build b))
     (let [b (.newBuilderForType ^com.google.protobuf.Message Leaf-prototype)]
       (codec/set-field! b Leaf--id (:id m) opts)
       (.build b)))))
(defn proto->Leaf
  "protobuf -> a Leaf record. Absent fields are nil."
  ([msg] (proto->Leaf msg nil))
  ([^com.google.protobuf.Message msg opts]
   (cond
     (and (nil? opts) (instance? com.acme.fixtures.wire.p2.Leaf msg))
     (let [^com.acme.fixtures.wire.p2.Leaf m msg]
       (->Leaf
        (when (.hasId m) (.getId m))
        ))

     (and (nil? opts) (rt/compiled-message? msg))
     (->Leaf
      (rt/slot msg 0)
      )

     :else
     (->Leaf
      (codec/get-field msg Leaf--id opts)
      ))))
(defn- proto->Leaf--map
  "Leaf as the plain map a nested field reads back as:
  the same values, minus the keys the codec's read leaves out."
  [^com.acme.fixtures.wire.p2.Leaf m]
  (let [id--v (when (.hasId m) (.getId m))]
    (if (some? id--v)
      {:id id--v}
      {})))
(defn- proto->Leaf--slot-map
  "Leaf as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [id--v (rt/slot msg 0)]
    (if (some? id--v)
      {:id id--v}
      {})))

(defrecord Wire [i32 i64 u32 u64 s32 s64 f32 f64 sf32 sf64 flt dbl flag str raw color leaf unpacked packed packed-s64 packed-dbl colors leaves names by-id by-color pick-str pick-i64 pick-leaf dflt-int dflt-str dflt-enum grp grps serialized-size class aliased high huge])
(def Wire-prototype (rt/message file-descriptor "Wire" "com.acme.fixtures.wire.p2.Wire"))
(def ^:private Wire--i32 (rt/field Wire-prototype "i32"))
(def ^:private Wire--i64 (rt/field Wire-prototype "i64"))
(def ^:private Wire--u32 (rt/field Wire-prototype "u32"))
(def ^:private Wire--u64 (rt/field Wire-prototype "u64"))
(def ^:private Wire--s32 (rt/field Wire-prototype "s32"))
(def ^:private Wire--s64 (rt/field Wire-prototype "s64"))
(def ^:private Wire--f32 (rt/field Wire-prototype "f32"))
(def ^:private Wire--f64 (rt/field Wire-prototype "f64"))
(def ^:private Wire--sf32 (rt/field Wire-prototype "sf32"))
(def ^:private Wire--sf64 (rt/field Wire-prototype "sf64"))
(def ^:private Wire--flt (rt/field Wire-prototype "flt"))
(def ^:private Wire--dbl (rt/field Wire-prototype "dbl"))
(def ^:private Wire--flag (rt/field Wire-prototype "flag"))
(def ^:private Wire--str (rt/field Wire-prototype "str"))
(def ^:private Wire--raw (rt/field Wire-prototype "raw"))
(def ^:private Wire--color (rt/field Wire-prototype "color"))
(def ^:private Wire--leaf (rt/field Wire-prototype "leaf"))
(def ^:private Wire--unpacked (rt/field Wire-prototype "unpacked"))
(def ^:private Wire--packed (rt/field Wire-prototype "packed"))
(def ^:private Wire--packed-s64 (rt/field Wire-prototype "packed_s64"))
(def ^:private Wire--packed-dbl (rt/field Wire-prototype "packed_dbl"))
(def ^:private Wire--colors (rt/field Wire-prototype "colors"))
(def ^:private Wire--leaves (rt/field Wire-prototype "leaves"))
(def ^:private Wire--names (rt/field Wire-prototype "names"))
(def ^:private Wire--by-id (rt/field Wire-prototype "by_id"))
(def ^:private Wire--by-color (rt/field Wire-prototype "by_color"))
(def ^:private Wire--pick-str (rt/field Wire-prototype "pick_str"))
(def ^:private Wire--pick-i64 (rt/field Wire-prototype "pick_i64"))
(def ^:private Wire--pick-leaf (rt/field Wire-prototype "pick_leaf"))
(def ^:private Wire--dflt-int (rt/field Wire-prototype "dflt_int"))
(def ^:private Wire--dflt-str (rt/field Wire-prototype "dflt_str"))
(def ^:private Wire--dflt-enum (rt/field Wire-prototype "dflt_enum"))
(def ^:private Wire--grp (rt/field Wire-prototype "grp"))
(def ^:private Wire--grps (rt/field Wire-prototype "grps"))
(def ^:private Wire--serialized-size (rt/field Wire-prototype "serialized_size"))
(def ^:private Wire--class (rt/field Wire-prototype "class"))
(def ^:private Wire--aliased (rt/field Wire-prototype "aliased"))
(def ^:private Wire--high (rt/field Wire-prototype "high"))
(def ^:private Wire--huge (rt/field Wire-prototype "huge"))
(defn Wire->proto
  "Clojure -> protobuf. Takes the Wire record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Wire->proto m nil))
  ([m opts]
   (if (nil? opts)
     (let [b (com.acme.fixtures.wire.p2.Wire/newBuilder)]
       (when-some [v (:i32 m)] (.setI32 b (int v)))
       (when-some [v (:i64 m)] (.setI64 b (long v)))
       (when-some [v (:u32 m)] (.setU32 b (int v)))
       (when-some [v (:u64 m)] (.setU64 b (long v)))
       (when-some [v (:s32 m)] (.setS32 b (int v)))
       (when-some [v (:s64 m)] (.setS64 b (long v)))
       (when-some [v (:f32 m)] (.setF32 b (int v)))
       (when-some [v (:f64 m)] (.setF64 b (long v)))
       (when-some [v (:sf32 m)] (.setSf32 b (int v)))
       (when-some [v (:sf64 m)] (.setSf64 b (long v)))
       (when-some [v (:flt m)] (.setFlt b (float v)))
       (when-some [v (:dbl m)] (.setDbl b (double v)))
       (when-some [v (:flag m)] (.setFlag b (boolean v)))
       (when-some [v (:str m)] (.setStr b ^String v))
       (when-some [v (:raw m)] (.setRaw b (if (bytes? v) (com.google.protobuf.ByteString/copyFrom ^bytes v) ^com.google.protobuf.ByteString v)))
       (codec/set-field! b Wire--color (:color m) nil)
       (when-some [v (:leaf m)] (if (map? v) (.setLeaf b ^com.acme.fixtures.wire.p2.Leaf (Leaf->proto v nil)) (codec/set-field! b Wire--leaf v nil)))
       (codec/set-field! b Wire--unpacked (:unpacked m) nil)
       (codec/set-field! b Wire--packed (:packed m) nil)
       (codec/set-field! b Wire--packed-s64 (:packed-s64 m) nil)
       (codec/set-field! b Wire--packed-dbl (:packed-dbl m) nil)
       (codec/set-field! b Wire--colors (:colors m) nil)
       (codec/set-field! b Wire--leaves (:leaves m) nil)
       (codec/set-field! b Wire--names (:names m) nil)
       (codec/set-field! b Wire--by-id (:by-id m) nil)
       (codec/set-field! b Wire--by-color (:by-color m) nil)
       (when-some [v (:pick-str m)] (.setPickStr b ^String v))
       (when-some [v (:pick-i64 m)] (.setPickI64 b (long v)))
       (when-some [v (:pick-leaf m)] (if (map? v) (.setPickLeaf b ^com.acme.fixtures.wire.p2.Leaf (Leaf->proto v nil)) (codec/set-field! b Wire--pick-leaf v nil)))
       (when-some [v (:dflt-int m)] (.setDfltInt b (int v)))
       (when-some [v (:dflt-str m)] (.setDfltStr b ^String v))
       (codec/set-field! b Wire--dflt-enum (:dflt-enum m) nil)
       (codec/set-field! b Wire--grp (:grp m) nil)
       (codec/set-field! b Wire--grps (:grps m) nil)
       (when-some [v (:serialized-size m)] (.setSerializedSize_ b (int v)))
       (when-some [v (:class m)] (.setClass_ b ^String v))
       (codec/set-field! b Wire--aliased (:aliased m) nil)
       (when-some [v (:high m)] (.setHigh b ^String v))
       (when-some [v (:huge m)] (.setHuge b (int v)))
       (.build b))
     (let [b (.newBuilderForType ^com.google.protobuf.Message Wire-prototype)]
       (codec/set-field! b Wire--i32 (:i32 m) opts)
       (codec/set-field! b Wire--i64 (:i64 m) opts)
       (codec/set-field! b Wire--u32 (:u32 m) opts)
       (codec/set-field! b Wire--u64 (:u64 m) opts)
       (codec/set-field! b Wire--s32 (:s32 m) opts)
       (codec/set-field! b Wire--s64 (:s64 m) opts)
       (codec/set-field! b Wire--f32 (:f32 m) opts)
       (codec/set-field! b Wire--f64 (:f64 m) opts)
       (codec/set-field! b Wire--sf32 (:sf32 m) opts)
       (codec/set-field! b Wire--sf64 (:sf64 m) opts)
       (codec/set-field! b Wire--flt (:flt m) opts)
       (codec/set-field! b Wire--dbl (:dbl m) opts)
       (codec/set-field! b Wire--flag (:flag m) opts)
       (codec/set-field! b Wire--str (:str m) opts)
       (codec/set-field! b Wire--raw (:raw m) opts)
       (codec/set-field! b Wire--color (:color m) opts)
       (codec/set-field! b Wire--leaf (:leaf m) opts)
       (codec/set-field! b Wire--unpacked (:unpacked m) opts)
       (codec/set-field! b Wire--packed (:packed m) opts)
       (codec/set-field! b Wire--packed-s64 (:packed-s64 m) opts)
       (codec/set-field! b Wire--packed-dbl (:packed-dbl m) opts)
       (codec/set-field! b Wire--colors (:colors m) opts)
       (codec/set-field! b Wire--leaves (:leaves m) opts)
       (codec/set-field! b Wire--names (:names m) opts)
       (codec/set-field! b Wire--by-id (:by-id m) opts)
       (codec/set-field! b Wire--by-color (:by-color m) opts)
       (codec/set-field! b Wire--pick-str (:pick-str m) opts)
       (codec/set-field! b Wire--pick-i64 (:pick-i64 m) opts)
       (codec/set-field! b Wire--pick-leaf (:pick-leaf m) opts)
       (codec/set-field! b Wire--dflt-int (:dflt-int m) opts)
       (codec/set-field! b Wire--dflt-str (:dflt-str m) opts)
       (codec/set-field! b Wire--dflt-enum (:dflt-enum m) opts)
       (codec/set-field! b Wire--grp (:grp m) opts)
       (codec/set-field! b Wire--grps (:grps m) opts)
       (codec/set-field! b Wire--serialized-size (:serialized-size m) opts)
       (codec/set-field! b Wire--class (:class m) opts)
       (codec/set-field! b Wire--aliased (:aliased m) opts)
       (codec/set-field! b Wire--high (:high m) opts)
       (codec/set-field! b Wire--huge (:huge m) opts)
       (.build b)))))
(defn proto->Wire
  "protobuf -> a Wire record. Absent fields are nil."
  ([msg] (proto->Wire msg nil))
  ([^com.google.protobuf.Message msg opts]
   (cond
     (and (nil? opts) (instance? com.acme.fixtures.wire.p2.Wire msg))
     (let [^com.acme.fixtures.wire.p2.Wire m msg]
       (->Wire
        (when (.hasI32 m) (.getI32 m))
        (when (.hasI64 m) (.getI64 m))
        (when (.hasU32 m) (.getU32 m))
        (when (.hasU64 m) (.getU64 m))
        (when (.hasS32 m) (.getS32 m))
        (when (.hasS64 m) (.getS64 m))
        (when (.hasF32 m) (.getF32 m))
        (when (.hasF64 m) (.getF64 m))
        (when (.hasSf32 m) (.getSf32 m))
        (when (.hasSf64 m) (.getSf64 m))
        (when (.hasFlt m) (.getFlt m))
        (when (.hasDbl m) (.getDbl m))
        (when (.hasFlag m) (.getFlag m))
        (when (.hasStr m) (.getStr m))
        (when (.hasRaw m) (.toByteArray (.getRaw m)))
        (when (.hasColor m) (case (.getNumber (.getColor m)) 0 :CLOSED_UNSPECIFIED 1 :CLOSED_A 2 :CLOSED_B (codec/get-field m Wire--color nil)))
        (when (.hasLeaf m) (proto->Leaf--map (.getLeaf m)))
        (let [l (.getUnpackedList m)] (when-not (.isEmpty l) (vec l)))
        (let [l (.getPackedList m)] (when-not (.isEmpty l) (vec l)))
        (let [l (.getPackedS64List m)] (when-not (.isEmpty l) (vec l)))
        (let [l (.getPackedDblList m)] (when-not (.isEmpty l) (vec l)))
        (codec/get-field m Wire--colors nil)
        (let [l (.getLeavesList m)] (when-not (.isEmpty l) (persistent! (reduce (fn [acc v] (conj! acc (proto->Leaf--map v))) (transient []) l))))
        (let [l (.getNamesList m)] (when-not (.isEmpty l) (vec l)))
        (let [jm (.getByIdMap m)] (when-not (.isEmpty jm) (persistent! (reduce (fn [acc ^java.util.Map$Entry e] (assoc! acc (.getKey e) (proto->Leaf--map (.getValue e)))) (transient {}) (.entrySet jm)))))
        (codec/get-field m Wire--by-color nil)
        (when (.hasPickStr m) (.getPickStr m))
        (when (.hasPickI64 m) (.getPickI64 m))
        (when (.hasPickLeaf m) (proto->Leaf--map (.getPickLeaf m)))
        (when (.hasDfltInt m) (.getDfltInt m))
        (when (.hasDfltStr m) (.getDfltStr m))
        (when (.hasDfltEnum m) (case (.getNumber (.getDfltEnum m)) 0 :CLOSED_UNSPECIFIED 1 :CLOSED_A 2 :CLOSED_B (codec/get-field m Wire--dflt-enum nil)))
        (codec/get-field m Wire--grp nil)
        (codec/get-field m Wire--grps nil)
        (when (.hasSerializedSize_ m) (.getSerializedSize_ m))
        (when (.hasClass_ m) (.getClass_ m))
        (codec/get-field m Wire--aliased nil)
        (when (.hasHigh m) (.getHigh m))
        (when (.hasHuge m) (.getHuge m))
        ))

     (and (nil? opts) (rt/compiled-message? msg))
     (->Wire
      (rt/slot msg 0)
      (rt/slot msg 1)
      (rt/slot msg 2)
      (rt/slot msg 3)
      (rt/slot msg 4)
      (rt/slot msg 5)
      (rt/slot msg 6)
      (rt/slot msg 7)
      (rt/slot msg 8)
      (rt/slot msg 9)
      (rt/slot msg 10)
      (rt/slot msg 11)
      (rt/slot msg 12)
      (rt/slot msg 13)
      (when-some [v (rt/slot msg 14)] (.toByteArray ^com.google.protobuf.ByteString v))
      (when-some [v (rt/slot msg 15)] (case v 0 :CLOSED_UNSPECIFIED 1 :CLOSED_A 2 :CLOSED_B (codec/get-field msg Wire--color nil)))
      (when-some [v (rt/slot msg 16)] (proto->Leaf--slot-map v))
      (let [^java.util.List l (rt/slot msg 17)] (when (and l (pos? (.size l))) (vec l)))
      (let [^java.util.List l (rt/slot msg 18)] (when (and l (pos? (.size l))) (vec l)))
      (let [^java.util.List l (rt/slot msg 19)] (when (and l (pos? (.size l))) (vec l)))
      (let [^java.util.List l (rt/slot msg 20)] (when (and l (pos? (.size l))) (vec l)))
      (codec/get-field msg Wire--colors nil)
      (let [^java.util.List l (rt/slot msg 22)] (when (and l (pos? (.size l))) (persistent! (reduce (fn [acc v] (conj! acc (proto->Leaf--slot-map v))) (transient []) l))))
      (let [^java.util.List l (rt/slot msg 23)] (when (and l (pos? (.size l))) (vec l)))
      (let [^java.util.Map jm (rt/slot msg 24)] (when (and jm (pos? (.size jm))) (persistent! (reduce (fn [acc ^java.util.Map$Entry e] (assoc! acc (.getKey e) (proto->Leaf--slot-map (.getValue e)))) (transient {}) (.entrySet jm)))))
      (codec/get-field msg Wire--by-color nil)
      (rt/slot msg 26)
      (rt/slot msg 27)
      (when-some [v (rt/slot msg 28)] (proto->Leaf--slot-map v))
      (rt/slot msg 29)
      (rt/slot msg 30)
      (when-some [v (rt/slot msg 31)] (case v 0 :CLOSED_UNSPECIFIED 1 :CLOSED_A 2 :CLOSED_B (codec/get-field msg Wire--dflt-enum nil)))
      (when-some [v (rt/slot msg 32)] (proto->Wire-Grp--slot-map v))
      (let [^java.util.List l (rt/slot msg 33)] (when (and l (pos? (.size l))) (persistent! (reduce (fn [acc v] (conj! acc (proto->Wire-Grps--slot-map v))) (transient []) l))))
      (rt/slot msg 34)
      (rt/slot msg 35)
      (codec/get-field msg Wire--aliased nil)
      (rt/slot msg 37)
      (rt/slot msg 38)
      )

     :else
     (->Wire
      (codec/get-field msg Wire--i32 opts)
      (codec/get-field msg Wire--i64 opts)
      (codec/get-field msg Wire--u32 opts)
      (codec/get-field msg Wire--u64 opts)
      (codec/get-field msg Wire--s32 opts)
      (codec/get-field msg Wire--s64 opts)
      (codec/get-field msg Wire--f32 opts)
      (codec/get-field msg Wire--f64 opts)
      (codec/get-field msg Wire--sf32 opts)
      (codec/get-field msg Wire--sf64 opts)
      (codec/get-field msg Wire--flt opts)
      (codec/get-field msg Wire--dbl opts)
      (codec/get-field msg Wire--flag opts)
      (codec/get-field msg Wire--str opts)
      (codec/get-field msg Wire--raw opts)
      (codec/get-field msg Wire--color opts)
      (codec/get-field msg Wire--leaf opts)
      (codec/get-field msg Wire--unpacked opts)
      (codec/get-field msg Wire--packed opts)
      (codec/get-field msg Wire--packed-s64 opts)
      (codec/get-field msg Wire--packed-dbl opts)
      (codec/get-field msg Wire--colors opts)
      (codec/get-field msg Wire--leaves opts)
      (codec/get-field msg Wire--names opts)
      (codec/get-field msg Wire--by-id opts)
      (codec/get-field msg Wire--by-color opts)
      (codec/get-field msg Wire--pick-str opts)
      (codec/get-field msg Wire--pick-i64 opts)
      (codec/get-field msg Wire--pick-leaf opts)
      (codec/get-field msg Wire--dflt-int opts)
      (codec/get-field msg Wire--dflt-str opts)
      (codec/get-field msg Wire--dflt-enum opts)
      (codec/get-field msg Wire--grp opts)
      (codec/get-field msg Wire--grps opts)
      (codec/get-field msg Wire--serialized-size opts)
      (codec/get-field msg Wire--class opts)
      (codec/get-field msg Wire--aliased opts)
      (codec/get-field msg Wire--high opts)
      (codec/get-field msg Wire--huge opts)
      ))))

(defrecord Wire-Grp [note])
(def Wire-Grp-prototype (rt/message file-descriptor "Wire.Grp" "com.acme.fixtures.wire.p2.Wire$Grp"))
(def ^:private Wire-Grp--note (rt/field Wire-Grp-prototype "note"))
(defn Wire-Grp->proto
  "Clojure -> protobuf. Takes the Wire-Grp record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Wire-Grp->proto m nil))
  ([m opts]
   (if (nil? opts)
     (let [b (com.acme.fixtures.wire.p2.Wire$Grp/newBuilder)]
       (when-some [v (:note m)] (.setNote b ^String v))
       (.build b))
     (let [b (.newBuilderForType ^com.google.protobuf.Message Wire-Grp-prototype)]
       (codec/set-field! b Wire-Grp--note (:note m) opts)
       (.build b)))))
(defn proto->Wire-Grp
  "protobuf -> a Wire-Grp record. Absent fields are nil."
  ([msg] (proto->Wire-Grp msg nil))
  ([^com.google.protobuf.Message msg opts]
   (cond
     (and (nil? opts) (instance? com.acme.fixtures.wire.p2.Wire$Grp msg))
     (let [^com.acme.fixtures.wire.p2.Wire$Grp m msg]
       (->Wire-Grp
        (when (.hasNote m) (.getNote m))
        ))

     (and (nil? opts) (rt/compiled-message? msg))
     (->Wire-Grp
      (rt/slot msg 0)
      )

     :else
     (->Wire-Grp
      (codec/get-field msg Wire-Grp--note opts)
      ))))
(defn- proto->Wire-Grp--slot-map
  "Wire-Grp as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [note--v (rt/slot msg 0)]
    (if (some? note--v)
      {:note note--v}
      {})))

(defrecord Wire-Grps [n])
(def Wire-Grps-prototype (rt/message file-descriptor "Wire.Grps" "com.acme.fixtures.wire.p2.Wire$Grps"))
(def ^:private Wire-Grps--n (rt/field Wire-Grps-prototype "n"))
(defn Wire-Grps->proto
  "Clojure -> protobuf. Takes the Wire-Grps record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Wire-Grps->proto m nil))
  ([m opts]
   (if (nil? opts)
     (let [b (com.acme.fixtures.wire.p2.Wire$Grps/newBuilder)]
       (when-some [v (:n m)] (.setN b (int v)))
       (.build b))
     (let [b (.newBuilderForType ^com.google.protobuf.Message Wire-Grps-prototype)]
       (codec/set-field! b Wire-Grps--n (:n m) opts)
       (.build b)))))
(defn proto->Wire-Grps
  "protobuf -> a Wire-Grps record. Absent fields are nil."
  ([msg] (proto->Wire-Grps msg nil))
  ([^com.google.protobuf.Message msg opts]
   (cond
     (and (nil? opts) (instance? com.acme.fixtures.wire.p2.Wire$Grps msg))
     (let [^com.acme.fixtures.wire.p2.Wire$Grps m msg]
       (->Wire-Grps
        (when (.hasN m) (.getN m))
        ))

     (and (nil? opts) (rt/compiled-message? msg))
     (->Wire-Grps
      (rt/slot msg 0)
      )

     :else
     (->Wire-Grps
      (codec/get-field msg Wire-Grps--n opts)
      ))))
(defn- proto->Wire-Grps--slot-map
  "Wire-Grps as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [n--v (rt/slot msg 0)]
    (if (some? n--v)
      {:n n--v}
      {})))

(defrecord Required [id n])
(def Required-prototype (rt/message file-descriptor "Required" "com.acme.fixtures.wire.p2.Required"))
(def ^:private Required--id (rt/field Required-prototype "id"))
(def ^:private Required--n (rt/field Required-prototype "n"))
(defn Required->proto
  "Clojure -> protobuf. Takes the Required record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Required->proto m nil))
  ([m opts]
   (if (nil? opts)
     (let [b (com.acme.fixtures.wire.p2.Required/newBuilder)]
       (when-some [v (:id m)] (.setId b ^String v))
       (when-some [v (:n m)] (.setN b (int v)))
       (.build b))
     (let [b (.newBuilderForType ^com.google.protobuf.Message Required-prototype)]
       (codec/set-field! b Required--id (:id m) opts)
       (codec/set-field! b Required--n (:n m) opts)
       (.build b)))))
(defn proto->Required
  "protobuf -> a Required record. Absent fields are nil."
  ([msg] (proto->Required msg nil))
  ([^com.google.protobuf.Message msg opts]
   (cond
     (and (nil? opts) (instance? com.acme.fixtures.wire.p2.Required msg))
     (let [^com.acme.fixtures.wire.p2.Required m msg]
       (->Required
        (when (.hasId m) (.getId m))
        (when (.hasN m) (.getN m))
        ))

     (and (nil? opts) (rt/compiled-message? msg))
     (->Required
      (rt/slot msg 0)
      (rt/slot msg 1)
      )

     :else
     (->Required
      (codec/get-field msg Required--id opts)
      (codec/get-field msg Required--n opts)
      ))))
