;; Generated by protoc-gen-clojure from fixtures/bench/shapes.proto. Do not edit.
;;
;; The FileDescriptorProto below is embedded verbatim; protobuf-java resolves
;; its edition features at load time, so this file needs no regeneration when
;; a new edition ships.
(ns fixtures.bench.shapes
  (:require [clj-protobuf.codec :as codec]
            [clj-protobuf.runtime :as rt]))

(def ^:private descriptor-b64
  "")

(def file-descriptor
  (rt/file-descriptor descriptor-b64
                      []))

;; ---------------------------------------------------------------
;; messages
(declare proto->Tiny--slot-map proto->Level2--slot-map proto->Level3--slot-map proto->Level4--slot-map proto->Payload--slot-map proto->Item--slot-map)
;;
;; The shape is known at codegen time, so the representation is too:
;; a defrecord per type, its FieldDescriptors resolved once into
;; vars, and straight-line ->proto/proto-> built on them. nil means
;; absent, which is how a record (all keys always present) maps onto
;; protobuf explicit presence.

(defrecord Flat [f1 f2 f3 f4 f5 f6 f7 f8 f9 f10 f11 f12])
(def Flat-prototype (rt/message file-descriptor "Flat" "com.acme.fixtures.bench.Flat"))
(def ^:private Flat--f1 (rt/field Flat-prototype "f1"))
(def ^:private Flat--f2 (rt/field Flat-prototype "f2"))
(def ^:private Flat--f3 (rt/field Flat-prototype "f3"))
(def ^:private Flat--f4 (rt/field Flat-prototype "f4"))
(def ^:private Flat--f5 (rt/field Flat-prototype "f5"))
(def ^:private Flat--f6 (rt/field Flat-prototype "f6"))
(def ^:private Flat--f7 (rt/field Flat-prototype "f7"))
(def ^:private Flat--f8 (rt/field Flat-prototype "f8"))
(def ^:private Flat--f9 (rt/field Flat-prototype "f9"))
(def ^:private Flat--f10 (rt/field Flat-prototype "f10"))
(def ^:private Flat--f11 (rt/field Flat-prototype "f11"))
(def ^:private Flat--f12 (rt/field Flat-prototype "f12"))
(defn Flat->proto
  "Clojure -> protobuf. Takes the Flat record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Flat->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Flat-prototype)]
     (codec/set-field! b Flat--f1 (:f1 m) opts)
     (codec/set-field! b Flat--f2 (:f2 m) opts)
     (codec/set-field! b Flat--f3 (:f3 m) opts)
     (codec/set-field! b Flat--f4 (:f4 m) opts)
     (codec/set-field! b Flat--f5 (:f5 m) opts)
     (codec/set-field! b Flat--f6 (:f6 m) opts)
     (codec/set-field! b Flat--f7 (:f7 m) opts)
     (codec/set-field! b Flat--f8 (:f8 m) opts)
     (codec/set-field! b Flat--f9 (:f9 m) opts)
     (codec/set-field! b Flat--f10 (:f10 m) opts)
     (codec/set-field! b Flat--f11 (:f11 m) opts)
     (codec/set-field! b Flat--f12 (:f12 m) opts)
     (.build b))))
(defn proto->Flat
  "protobuf -> a Flat record. Absent fields are nil."
  ([msg] (proto->Flat msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Flat
      (rt/slot msg 0)
      (rt/slot msg 1)
      (rt/slot msg 2)
      (rt/slot msg 3)
      (rt/slot msg 4)
      (rt/slot msg 5)
      (rt/slot msg 6)
      (rt/slot msg 7)
      (rt/slot msg 8)
      (rt/slot msg 9)
      (rt/slot msg 10)
      (rt/slot msg 11)
      )
     (->Flat
      (codec/get-field msg Flat--f1 opts)
      (codec/get-field msg Flat--f2 opts)
      (codec/get-field msg Flat--f3 opts)
      (codec/get-field msg Flat--f4 opts)
      (codec/get-field msg Flat--f5 opts)
      (codec/get-field msg Flat--f6 opts)
      (codec/get-field msg Flat--f7 opts)
      (codec/get-field msg Flat--f8 opts)
      (codec/get-field msg Flat--f9 opts)
      (codec/get-field msg Flat--f10 opts)
      (codec/get-field msg Flat--f11 opts)
      (codec/get-field msg Flat--f12 opts)
      ))))

(defrecord Tiny [id n ok])
(def Tiny-prototype (rt/message file-descriptor "Tiny" "com.acme.fixtures.bench.Tiny"))
(def ^:private Tiny--id (rt/field Tiny-prototype "id"))
(def ^:private Tiny--n (rt/field Tiny-prototype "n"))
(def ^:private Tiny--ok (rt/field Tiny-prototype "ok"))
(defn Tiny->proto
  "Clojure -> protobuf. Takes the Tiny record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Tiny->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Tiny-prototype)]
     (codec/set-field! b Tiny--id (:id m) opts)
     (codec/set-field! b Tiny--n (:n m) opts)
     (codec/set-field! b Tiny--ok (:ok m) opts)
     (.build b))))
(defn proto->Tiny
  "protobuf -> a Tiny record. Absent fields are nil."
  ([msg] (proto->Tiny msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Tiny
      (rt/slot msg 0)
      (rt/slot msg 1)
      (rt/slot msg 2)
      )
     (->Tiny
      (codec/get-field msg Tiny--id opts)
      (codec/get-field msg Tiny--n opts)
      (codec/get-field msg Tiny--ok opts)
      ))))
(defn- proto->Tiny--slot-map
  "Tiny as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [id--v (rt/slot msg 0)
        n--v (rt/slot msg 1)
        ok--v (rt/slot msg 2)]
    (if (and (some? id--v) (some? n--v) (some? ok--v))
      {:id id--v
       :n n--v
       :ok ok--v}
      (persistent!
       (cond-> (transient {})
         (some? id--v) (assoc! :id id--v)
         (some? n--v) (assoc! :n n--v)
         (some? ok--v) (assoc! :ok ok--v)
         )))))

(defrecord Deep [id child])
(def Deep-prototype (rt/message file-descriptor "Deep" "com.acme.fixtures.bench.Deep"))
(def ^:private Deep--id (rt/field Deep-prototype "id"))
(def ^:private Deep--child (rt/field Deep-prototype "child"))
(defn Deep->proto
  "Clojure -> protobuf. Takes the Deep record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Deep->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Deep-prototype)]
     (codec/set-field! b Deep--id (:id m) opts)
     (codec/set-field! b Deep--child (:child m) opts)
     (.build b))))
(defn proto->Deep
  "protobuf -> a Deep record. Absent fields are nil."
  ([msg] (proto->Deep msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Deep
      (rt/slot msg 0)
      (when-some [v (rt/slot msg 1)] (proto->Level2--slot-map v))
      )
     (->Deep
      (codec/get-field msg Deep--id opts)
      (codec/get-field msg Deep--child opts)
      ))))

(defrecord Level2 [id child])
(def Level2-prototype (rt/message file-descriptor "Level2" "com.acme.fixtures.bench.Level2"))
(def ^:private Level2--id (rt/field Level2-prototype "id"))
(def ^:private Level2--child (rt/field Level2-prototype "child"))
(defn Level2->proto
  "Clojure -> protobuf. Takes the Level2 record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Level2->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Level2-prototype)]
     (codec/set-field! b Level2--id (:id m) opts)
     (codec/set-field! b Level2--child (:child m) opts)
     (.build b))))
(defn proto->Level2
  "protobuf -> a Level2 record. Absent fields are nil."
  ([msg] (proto->Level2 msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Level2
      (rt/slot msg 0)
      (when-some [v (rt/slot msg 1)] (proto->Level3--slot-map v))
      )
     (->Level2
      (codec/get-field msg Level2--id opts)
      (codec/get-field msg Level2--child opts)
      ))))
(defn- proto->Level2--slot-map
  "Level2 as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [id--v (rt/slot msg 0)
        child--v (when-some [v (rt/slot msg 1)] (proto->Level3--slot-map v))]
    (if (and (some? id--v) (some? child--v))
      {:id id--v
       :child child--v}
      (persistent!
       (cond-> (transient {})
         (some? id--v) (assoc! :id id--v)
         (some? child--v) (assoc! :child child--v)
         )))))

(defrecord Level3 [id child])
(def Level3-prototype (rt/message file-descriptor "Level3" "com.acme.fixtures.bench.Level3"))
(def ^:private Level3--id (rt/field Level3-prototype "id"))
(def ^:private Level3--child (rt/field Level3-prototype "child"))
(defn Level3->proto
  "Clojure -> protobuf. Takes the Level3 record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Level3->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Level3-prototype)]
     (codec/set-field! b Level3--id (:id m) opts)
     (codec/set-field! b Level3--child (:child m) opts)
     (.build b))))
(defn proto->Level3
  "protobuf -> a Level3 record. Absent fields are nil."
  ([msg] (proto->Level3 msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Level3
      (rt/slot msg 0)
      (when-some [v (rt/slot msg 1)] (proto->Level4--slot-map v))
      )
     (->Level3
      (codec/get-field msg Level3--id opts)
      (codec/get-field msg Level3--child opts)
      ))))
(defn- proto->Level3--slot-map
  "Level3 as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [id--v (rt/slot msg 0)
        child--v (when-some [v (rt/slot msg 1)] (proto->Level4--slot-map v))]
    (if (and (some? id--v) (some? child--v))
      {:id id--v
       :child child--v}
      (persistent!
       (cond-> (transient {})
         (some? id--v) (assoc! :id id--v)
         (some? child--v) (assoc! :child child--v)
         )))))

(defrecord Level4 [id leaf])
(def Level4-prototype (rt/message file-descriptor "Level4" "com.acme.fixtures.bench.Level4"))
(def ^:private Level4--id (rt/field Level4-prototype "id"))
(def ^:private Level4--leaf (rt/field Level4-prototype "leaf"))
(defn Level4->proto
  "Clojure -> protobuf. Takes the Level4 record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Level4->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Level4-prototype)]
     (codec/set-field! b Level4--id (:id m) opts)
     (codec/set-field! b Level4--leaf (:leaf m) opts)
     (.build b))))
(defn proto->Level4
  "protobuf -> a Level4 record. Absent fields are nil."
  ([msg] (proto->Level4 msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Level4
      (rt/slot msg 0)
      (rt/slot msg 1)
      )
     (->Level4
      (codec/get-field msg Level4--id opts)
      (codec/get-field msg Level4--leaf opts)
      ))))
(defn- proto->Level4--slot-map
  "Level4 as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [id--v (rt/slot msg 0)
        leaf--v (rt/slot msg 1)]
    (if (and (some? id--v) (some? leaf--v))
      {:id id--v
       :leaf leaf--v}
      (persistent!
       (cond-> (transient {})
         (some? id--v) (assoc! :id id--v)
         (some? leaf--v) (assoc! :leaf leaf--v)
         )))))

(defrecord WideRepeated [id items])
(def WideRepeated-prototype (rt/message file-descriptor "WideRepeated" "com.acme.fixtures.bench.WideRepeated"))
(def ^:private WideRepeated--id (rt/field WideRepeated-prototype "id"))
(def ^:private WideRepeated--items (rt/field WideRepeated-prototype "items"))
(defn WideRepeated->proto
  "Clojure -> protobuf. Takes the WideRepeated record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (WideRepeated->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message WideRepeated-prototype)]
     (codec/set-field! b WideRepeated--id (:id m) opts)
     (codec/set-field! b WideRepeated--items (:items m) opts)
     (.build b))))
(defn proto->WideRepeated
  "protobuf -> a WideRepeated record. Absent fields are nil."
  ([msg] (proto->WideRepeated msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->WideRepeated
      (rt/slot msg 0)
      (let [^java.util.List l (rt/slot msg 1)] (when (and l (pos? (.size l))) (vec l)))
      )
     (->WideRepeated
      (codec/get-field msg WideRepeated--id opts)
      (codec/get-field msg WideRepeated--items opts)
      ))))

(defrecord RepeatedMessages [id rows])
(def RepeatedMessages-prototype (rt/message file-descriptor "RepeatedMessages" "com.acme.fixtures.bench.RepeatedMessages"))
(def ^:private RepeatedMessages--id (rt/field RepeatedMessages-prototype "id"))
(def ^:private RepeatedMessages--rows (rt/field RepeatedMessages-prototype "rows"))
(defn RepeatedMessages->proto
  "Clojure -> protobuf. Takes the RepeatedMessages record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (RepeatedMessages->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message RepeatedMessages-prototype)]
     (codec/set-field! b RepeatedMessages--id (:id m) opts)
     (codec/set-field! b RepeatedMessages--rows (:rows m) opts)
     (.build b))))
(defn proto->RepeatedMessages
  "protobuf -> a RepeatedMessages record. Absent fields are nil."
  ([msg] (proto->RepeatedMessages msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->RepeatedMessages
      (rt/slot msg 0)
      (let [^java.util.List l (rt/slot msg 1)] (when (and l (pos? (.size l))) (persistent! (reduce (fn [acc v] (conj! acc (proto->Tiny--slot-map v))) (transient []) l))))
      )
     (->RepeatedMessages
      (codec/get-field msg RepeatedMessages--id opts)
      (codec/get-field msg RepeatedMessages--rows opts)
      ))))

(defrecord MapHeavy [id counts])
(def MapHeavy-prototype (rt/message file-descriptor "MapHeavy" "com.acme.fixtures.bench.MapHeavy"))
(def ^:private MapHeavy--id (rt/field MapHeavy-prototype "id"))
(def ^:private MapHeavy--counts (rt/field MapHeavy-prototype "counts"))
(defn MapHeavy->proto
  "Clojure -> protobuf. Takes the MapHeavy record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (MapHeavy->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message MapHeavy-prototype)]
     (codec/set-field! b MapHeavy--id (:id m) opts)
     (codec/set-field! b MapHeavy--counts (:counts m) opts)
     (.build b))))
(defn proto->MapHeavy
  "protobuf -> a MapHeavy record. Absent fields are nil."
  ([msg] (proto->MapHeavy msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->MapHeavy
      (rt/slot msg 0)
      (let [^java.util.Map jm (rt/slot msg 1)] (when (and jm (pos? (.size jm))) (persistent! (reduce (fn [acc ^java.util.Map$Entry e] (assoc! acc (.getKey e) (.getValue e))) (transient {}) (.entrySet jm)))))
      )
     (->MapHeavy
      (codec/get-field msg MapHeavy--id opts)
      (codec/get-field msg MapHeavy--counts opts)
      ))))

(defrecord EnumHeavy [id s1 s2 s3 s4 s5 s6 s7 s8 history])
(def EnumHeavy-prototype (rt/message file-descriptor "EnumHeavy" "com.acme.fixtures.bench.EnumHeavy"))
(def ^:private EnumHeavy--id (rt/field EnumHeavy-prototype "id"))
(def ^:private EnumHeavy--s1 (rt/field EnumHeavy-prototype "s1"))
(def ^:private EnumHeavy--s2 (rt/field EnumHeavy-prototype "s2"))
(def ^:private EnumHeavy--s3 (rt/field EnumHeavy-prototype "s3"))
(def ^:private EnumHeavy--s4 (rt/field EnumHeavy-prototype "s4"))
(def ^:private EnumHeavy--s5 (rt/field EnumHeavy-prototype "s5"))
(def ^:private EnumHeavy--s6 (rt/field EnumHeavy-prototype "s6"))
(def ^:private EnumHeavy--s7 (rt/field EnumHeavy-prototype "s7"))
(def ^:private EnumHeavy--s8 (rt/field EnumHeavy-prototype "s8"))
(def ^:private EnumHeavy--history (rt/field EnumHeavy-prototype "history"))
(defn EnumHeavy->proto
  "Clojure -> protobuf. Takes the EnumHeavy record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (EnumHeavy->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message EnumHeavy-prototype)]
     (codec/set-field! b EnumHeavy--id (:id m) opts)
     (codec/set-field! b EnumHeavy--s1 (:s1 m) opts)
     (codec/set-field! b EnumHeavy--s2 (:s2 m) opts)
     (codec/set-field! b EnumHeavy--s3 (:s3 m) opts)
     (codec/set-field! b EnumHeavy--s4 (:s4 m) opts)
     (codec/set-field! b EnumHeavy--s5 (:s5 m) opts)
     (codec/set-field! b EnumHeavy--s6 (:s6 m) opts)
     (codec/set-field! b EnumHeavy--s7 (:s7 m) opts)
     (codec/set-field! b EnumHeavy--s8 (:s8 m) opts)
     (codec/set-field! b EnumHeavy--history (:history m) opts)
     (.build b))))
(defn proto->EnumHeavy
  "protobuf -> a EnumHeavy record. Absent fields are nil."
  ([msg] (proto->EnumHeavy msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->EnumHeavy
      (rt/slot msg 0)
      (when-some [v (rt/slot msg 1)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s1 nil)))
      (when-some [v (rt/slot msg 2)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s2 nil)))
      (when-some [v (rt/slot msg 3)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s3 nil)))
      (when-some [v (rt/slot msg 4)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s4 nil)))
      (when-some [v (rt/slot msg 5)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s5 nil)))
      (when-some [v (rt/slot msg 6)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s6 nil)))
      (when-some [v (rt/slot msg 7)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s7 nil)))
      (when-some [v (rt/slot msg 8)] (case v 0 :STATUS_UNSPECIFIED 1 :STATUS_ACTIVE 2 :STATUS_PAUSED 3 :STATUS_CLOSED (codec/get-field msg EnumHeavy--s8 nil)))
      (codec/get-field msg EnumHeavy--history nil)
      )
     (->EnumHeavy
      (codec/get-field msg EnumHeavy--id opts)
      (codec/get-field msg EnumHeavy--s1 opts)
      (codec/get-field msg EnumHeavy--s2 opts)
      (codec/get-field msg EnumHeavy--s3 opts)
      (codec/get-field msg EnumHeavy--s4 opts)
      (codec/get-field msg EnumHeavy--s5 opts)
      (codec/get-field msg EnumHeavy--s6 opts)
      (codec/get-field msg EnumHeavy--s7 opts)
      (codec/get-field msg EnumHeavy--s8 opts)
      (codec/get-field msg EnumHeavy--history opts)
      ))))

(defrecord Reply [name payload])
(def Reply-prototype (rt/message file-descriptor "Reply" "com.acme.fixtures.bench.Reply"))
(def ^:private Reply--name (rt/field Reply-prototype "name"))
(def ^:private Reply--payload (rt/field Reply-prototype "payload"))
(defn Reply->proto
  "Clojure -> protobuf. Takes the Reply record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Reply->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Reply-prototype)]
     (codec/set-field! b Reply--name (:name m) opts)
     (codec/set-field! b Reply--payload (:payload m) opts)
     (.build b))))
(defn proto->Reply
  "protobuf -> a Reply record. Absent fields are nil."
  ([msg] (proto->Reply msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Reply
      (rt/slot msg 0)
      (when-some [v (rt/slot msg 1)] (proto->Payload--slot-map v))
      )
     (->Reply
      (codec/get-field msg Reply--name opts)
      (codec/get-field msg Reply--payload opts)
      ))))

(defrecord Payload [id title body created-at score items])
(def Payload-prototype (rt/message file-descriptor "Payload" "com.acme.fixtures.bench.Payload"))
(def ^:private Payload--id (rt/field Payload-prototype "id"))
(def ^:private Payload--title (rt/field Payload-prototype "title"))
(def ^:private Payload--body (rt/field Payload-prototype "body"))
(def ^:private Payload--created-at (rt/field Payload-prototype "created_at"))
(def ^:private Payload--score (rt/field Payload-prototype "score"))
(def ^:private Payload--items (rt/field Payload-prototype "items"))
(defn Payload->proto
  "Clojure -> protobuf. Takes the Payload record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Payload->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Payload-prototype)]
     (codec/set-field! b Payload--id (:id m) opts)
     (codec/set-field! b Payload--title (:title m) opts)
     (codec/set-field! b Payload--body (:body m) opts)
     (codec/set-field! b Payload--created-at (:created-at m) opts)
     (codec/set-field! b Payload--score (:score m) opts)
     (codec/set-field! b Payload--items (:items m) opts)
     (.build b))))
(defn proto->Payload
  "protobuf -> a Payload record. Absent fields are nil."
  ([msg] (proto->Payload msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Payload
      (rt/slot msg 0)
      (rt/slot msg 1)
      (rt/slot msg 2)
      (rt/slot msg 3)
      (rt/slot msg 4)
      (let [^java.util.List l (rt/slot msg 5)] (when (and l (pos? (.size l))) (persistent! (reduce (fn [acc v] (conj! acc (proto->Item--slot-map v))) (transient []) l))))
      )
     (->Payload
      (codec/get-field msg Payload--id opts)
      (codec/get-field msg Payload--title opts)
      (codec/get-field msg Payload--body opts)
      (codec/get-field msg Payload--created-at opts)
      (codec/get-field msg Payload--score opts)
      (codec/get-field msg Payload--items opts)
      ))))
(defn- proto->Payload--slot-map
  "Payload as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [id--v (rt/slot msg 0)
        title--v (rt/slot msg 1)
        body--v (rt/slot msg 2)
        created-at--v (rt/slot msg 3)
        score--v (rt/slot msg 4)
        items--v (let [^java.util.List l (rt/slot msg 5)] (when (and l (pos? (.size l))) (persistent! (reduce (fn [acc v] (conj! acc (proto->Item--slot-map v))) (transient []) l))))]
    (if (and (some? id--v) (some? title--v) (some? body--v) (some? created-at--v) (some? score--v) (some? items--v))
      {:id id--v
       :title title--v
       :body body--v
       :created-at created-at--v
       :score score--v
       :items items--v}
      (persistent!
       (cond-> (transient {})
         (some? id--v) (assoc! :id id--v)
         (some? title--v) (assoc! :title title--v)
         (some? body--v) (assoc! :body body--v)
         (some? created-at--v) (assoc! :created-at created-at--v)
         (some? score--v) (assoc! :score score--v)
         (some? items--v) (assoc! :items items--v)
         )))))

(defrecord Item [sku qty price])
(def Item-prototype (rt/message file-descriptor "Item" "com.acme.fixtures.bench.Item"))
(def ^:private Item--sku (rt/field Item-prototype "sku"))
(def ^:private Item--qty (rt/field Item-prototype "qty"))
(def ^:private Item--price (rt/field Item-prototype "price"))
(defn Item->proto
  "Clojure -> protobuf. Takes the Item record or any map
  with the same keys — records and plain maps are interchangeable."
  ([m] (Item->proto m nil))
  ([m opts]
   (let [b (.newBuilderForType ^com.google.protobuf.Message Item-prototype)]
     (codec/set-field! b Item--sku (:sku m) opts)
     (codec/set-field! b Item--qty (:qty m) opts)
     (codec/set-field! b Item--price (:price m) opts)
     (.build b))))
(defn proto->Item
  "protobuf -> a Item record. Absent fields are nil."
  ([msg] (proto->Item msg nil))
  ([^com.google.protobuf.Message msg opts]
   (if (and (nil? opts) (rt/compiled-message? msg))
     (->Item
      (rt/slot msg 0)
      (rt/slot msg 1)
      (rt/slot msg 2)
      )
     (->Item
      (codec/get-field msg Item--sku opts)
      (codec/get-field msg Item--qty opts)
      (codec/get-field msg Item--price opts)
      ))))
(defn- proto->Item--slot-map
  "Item as a plain map, read from the compiled arm's slots:
  the same values, minus the keys the codec's read leaves out."
  [msg]
  (let [sku--v (rt/slot msg 0)
        qty--v (rt/slot msg 1)
        price--v (rt/slot msg 2)]
    (if (and (some? sku--v) (some? qty--v) (some? price--v))
      {:sku sku--v
       :qty qty--v
       :price price--v}
      (persistent!
       (cond-> (transient {})
         (some? sku--v) (assoc! :sku sku--v)
         (some? qty--v) (assoc! :qty qty--v)
         (some? price--v) (assoc! :price price--v)
         )))))
